#include <stdio.h>
/**
 * main - prints in lowercase,
 * followed by new line
 * Return: 0 (Accomplished)
 */
int main(void)
{
	char ch;

	

	for 
	(ch = 'a'; i ch < = 'z'; ch++);
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
